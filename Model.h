#ifndef MODEL_H
#define MODEL_H

template <typename T>
class Model
{
	private:
		// Private fields and functions for model

	public:
		// Public functions for model

		/**
		 * @brief Empty Model constructor
		 * @todo Write this functions
		 */
		Model()
		{
			
		}


		/**
		 * @brief Classify an input key as hor or cold
		 * @param key The key to be classified
		 * @return True if the key is hot and false otherwise
		 */
		bool classify(T key)
		{
			return false;
		}
}

#endif
